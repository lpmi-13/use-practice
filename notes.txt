- would it be possible to distinguish (and simulate) varying cpu saturation scenarios based on whether it's too many runnable threads or I/O Wait (D-State) threads? And if so, should we also bake that distinction into the use-tool (or is this dealt with in a separate part of Brendan Gregg's work...maybe on-cpu vs off-cpu profiling?)
- should we configure some swap space on the playground by default?
- let's update the use-practice command to work the same way as the select mode of use-tool, where we can run `use-practice run` and get a select list of random/cpu/memory/disk/network, and also when running a specific resource scenario type we also get a dropdown for which random/scenario1/scenario2/etc.
